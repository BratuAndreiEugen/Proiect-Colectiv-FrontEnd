import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: "#E4A027",
    },
    secondary: {
      main: "#556b2f",
    },
  },
});

export default theme;
