import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: "#ff6633",
    },
    secondary: {
      main: "#556b2f",
    },
  },
});

export default theme;
