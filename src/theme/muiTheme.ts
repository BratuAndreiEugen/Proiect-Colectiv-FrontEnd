import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: "#fb8846",
    },
    secondary: {
      main: "#556b2f",
    },
  },
});

export default theme;
